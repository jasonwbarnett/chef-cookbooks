# This file is maintained by Chef. Do not edit, all changes will be
# overwritten. See bw_systemd/README.md

[<%= @section %>]
<% if node['bw_systemd'][@config]['config']
     keys = node['bw_systemd'][@config]['config'].to_hash
   else
     keys = node['bw_systemd'][@config].to_hash
   end
   keys.sort.each do |key, val|
     if val.is_a?(TrueClass)
       v = 'yes'
     elsif val.is_a?(FalseClass)
       v = 'no'
     elsif val.is_a?(Array)
       v = val.join(' ')
     else
       v = val
     end %>
<%=  key %>=<%= v %>
<% end %>
